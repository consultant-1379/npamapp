<arquillian>

    <extension qualifier="graphene">
        <property name="url">${app.url}</property>
        <property name="waitGuiInterval">30</property>
    </extension>

    <extension qualifier="webdriver">
       <property name="browser">${browser.name}</property>
    <!--    <property name="browser">chromeHeadless</property> -->
        <property name="dimensions">full</property>
    </extension>

</arquillian>
